name: Topic News Collector

on:
  workflow_dispatch:
    inputs:
      run_mode:
        description: "Choose run mode"
        required: true
        default: "daily"
        type: choice
        options:
          - daily
          - weekly

permissions:
  contents: write

jobs:
  daily:
    concurrency:
      group: topic-feed-daily
      cancel-in-progress: false

    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 7 * * *') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_mode == 'daily')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/lib/android
          sudo apt-get clean

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run script (daily)
        env:
          RUN_MODE: daily
          PAST_DAYS_DAILY: "1"
          PAST_DAYS_WEEKLY: "7"
          MAX_ITEMS: "50"
          DUP_THRESHOLD: "0.60"
          MODEL_NAME: "all-MiniLM-L6-v2"
        run: |
          python main.py

      - name: List output files
        run: |
          echo "==== data tree ===="
          ls -R data || true
          echo "==== docs tree ===="
          ls -R docs || true

      - name: Commit outputs (daily)
        shell: bash
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          files=()
          [ -f data/topic_feeds_latest.xlsx ] && files+=(data/topic_feeds_latest.xlsx)
          [ -f data/topic_feeds_rolling_7d.xlsx ] && files+=(data/topic_feeds_rolling_7d.xlsx)
          [ -f docs/feed.json ]        && files+=(docs/feed.json)
          [ -f docs/daily_feed.json ]  && files+=(docs/daily_feed.json)
          [ -f docs/weekly_feed.json ] && files+=(docs/weekly_feed.json)

          shopt -s nullglob
          daily_files=(data/daily/*.xlsx)
          shopt -u nullglob
          [ ${#daily_files[@]} -gt 0 ] && files+=("${daily_files[@]}")

          [ ${#files[@]} -eq 0 ] && echo "No output files found." && exit 0

          git add "${files[@]}"
          git diff --cached --quiet && echo "No changes to commit." && exit 0

          git commit -m "Update topic news feed (daily)"
          git stash || true
          git pull --rebase origin main
          git stash pop || true
          git push origin HEAD:main

  weekly:
    concurrency:
      group: topic-feed-weekly
      cancel-in-progress: false

    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 7 * * 3') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_mode == 'weekly')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/lib/android
          sudo apt-get clean

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run script (weekly)
        env:
          RUN_MODE: weekly
          PAST_DAYS_DAILY: "1"
          PAST_DAYS_WEEKLY: "7"
          MAX_ITEMS: "50"
          DUP_THRESHOLD: "0.60"
          MODEL_NAME: "all-MiniLM-L6-v2"
        run: |
          python main.py

      - name: List output files
        run: |
          echo "==== data tree ===="
          ls -R data || true
          echo "==== docs tree ===="
          ls -R docs || true

      - name: Commit outputs (weekly)
        shell: bash
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          files=()
          [ -f data/topic_feeds_latest.xlsx ] && files+=(data/topic_feeds_latest.xlsx)
          [ -f data/topic_feeds_rolling_7d.xlsx ] && files+=(data/topic_feeds_rolling_7d.xlsx)
          [ -f docs/feed.json ]        && files+=(docs/feed.json)
          [ -f docs/daily_feed.json ]  && files+=(docs/daily_feed.json)
          [ -f docs/weekly_feed.json ] && files+=(docs/weekly_feed.json)

          shopt -s nullglob
          weekly_files=(data/weekly/*.xlsx)
          shopt -u nullglob
          [ ${#weekly_files[@]} -gt 0 ] && files+=("${weekly_files[@]}")

          [ ${#files[@]} -eq 0 ] && echo "No output files found." && exit 0

          git add "${files[@]}"
          git diff --cached --quiet && echo "No changes to commit." && exit 0

          git commit -m "Update topic news feed (weekly)"
          git stash || true
          git pull --rebase origin main
          git stash pop || true
          git push origin HEAD:main
